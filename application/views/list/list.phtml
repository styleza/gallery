<?php if(!$this->images): ?>
<div class="alert alert-danger"><?=$this->translate('nothing_to_show')?></div>
<?php else: ?>
<div class="row">
    <?php foreach($this->images as $image){
       echo $this->partial('views/list/object.phtml',array('photo' => $image["photo"], 'controls' => $this->isownpage));
    } ?>
</div>
<?php endif;?>